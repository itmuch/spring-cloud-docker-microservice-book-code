server:
  port: 8888
spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          uri: https://git.oschina.net/itmuch/spring-cloud-config-repo      # 配置git仓库的地址
          username:                                                         # git仓库的账号
          password:                                                         # git仓库的密码